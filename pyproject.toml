[tool.poetry]
name = "ReCAP-Utils"
version = "0.1.19"
description = ""
authors = ["Mirko Lenz <info@mirko-lenz.de>"]

[tool.poetry.scripts]
recap-utils = "recap_utils.app:cli"

[tool.poetry.dependencies]
python = "^3.8"
recap-argument-graph = "^0.1.37"
click = "^7.0"
tomlkit = "^0.7"
click-pathlib = "^2020.3"
deepl-pro = "^0.1.4"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
